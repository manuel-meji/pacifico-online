openapi: 3.0.3
info:
  title: Contrato de Integración - m2-comercios
  description: Especificación OpenAPI v1 para el módulo m2-comercios en Pacíficos Online.
  version: 1.0.0
servers:
  - url: /api/v1/m2-comercios
    description: Servidor local / integración
paths:
  /health:
    get:
      summary: Verificación de estado del módulo
      responses:
        '200':
          description: Módulo operando correctamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  module:
                    type: string
                    example: m2-comercios
